DOM
DOMTree
-> DOM의 조작 가하면 -> DOMTree 구성 -> CSS 관계 설정 -> 배치

트랜스파일러
JSX -> JS

-> 개발 서버

node.js : 자바스크립트 런타임 - V8

node : 자바스크립트 코드 실행
   node 자바스크립트 파일
   
   npm : (node package manager)
         - 의존성 관리(프로젝트에서 필요한 라이브러리)
         - 빌드 : 개발 소스 -> 서비스(배포) 가능 상태로 변경
                                    (리액트 문법 소스 -> js)
         - 테스트
         
         install -> 설치
         설치 : npm install 패키지명
         -> 설치, 현재 프로젝트의node_modules 폴더에 설치 됨
            == npm i 패키지명
            
         삭제 : npm uninstall 패키지명 -> 삭제
         
         npm --global install 패키지
         -> 모든 프로젝트가 공유 가능한 위치에 설치
            == npm -g i 패키지명
            
         npmjs.org
         
         init : 프로젝트 생성
         
   yarn - npm과 동일 역할 // 개발사 페이스북
      - 메타쪽에서 개발
      - 설치 속도 빠르다
      - UI가 깔끔
      
      create : 프로젝트 생성
      add 패키지명
      
      설치
      
      npm i -g yarn


      npm i - > package 